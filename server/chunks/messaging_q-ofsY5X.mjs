globalThis.process ??= {};
globalThis.process.env ??= {};
import { c as createComponent } from "./astro-component_Bc18R3r1.mjs";
import { r as renderTemplate, u as unescapeHTML, a as addAttribute, F as Fragment, m as maybeRenderHead } from "./sequence_BESBTeYg.mjs";
import { r as renderComponent } from "./worker-entry_EC1jLQM3.mjs";
import { env } from "cloudflare:workers";
import { $ as $$App } from "./App__9dDIE7_.mjs";
var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a, _b;
const prerender = false;
const $$Messaging = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$props, $$slots);
  Astro2.self = $$Messaging;
  const { hasApiKey, getApiKey } = await import("./client_DbLECgB2.mjs");
  const { discordInviteUrl } = await import("./discord_DPe7Z3mk.mjs");
  const { saveMark, connLabel } = await import("./conn-status_DKuiC5qX.mjs");
  const connLabels = { saved: connLabel("saved"), pending: connLabel("pending"), warning: connLabel("warning"), error: connLabel("error") };
  const dcAppId = await getApiKey(env, "discord_app_id");
  const dcInviteUrl = dcAppId ? discordInviteUrl(dcAppId) : null;
  const dcInviteUrlNoCreate = dcAppId ? discordInviteUrl(dcAppId, { manageChannels: false }) : null;
  const status = {
    discord_app_id: await hasApiKey(env, "discord_app_id"),
    discord_public_key: await hasApiKey(env, "discord_public_key"),
    discord_bot_token: await hasApiKey(env, "discord_bot_token"),
    discord_client_secret: await hasApiKey(env, "discord_client_secret"),
    slack_signing_secret: await hasApiKey(env, "slack_signing_secret"),
    slack_bot_token: await hasApiKey(env, "slack_bot_token"),
    line_secret: await hasApiKey(env, "line_secret"),
    line_token: await hasApiKey(env, "line_token")
  };
  const mark = saveMark;
  const O = Astro2.url.origin;
  const svcReady = { discord: status.discord_bot_token, slack: status.slack_bot_token, line: status.line_token };
  const { getNotifyWebhook } = await import("./settings_DI_y7gTJ.mjs");
  const notifyWebhookSet = !!await getNotifyWebhook(env).catch(() => "");
  let lineLinked = 0;
  let lineQuotaInfo = null;
  if (status.line_token) {
    const [{ linkedLineMembers }, { getApiKey: getApiKey2 }] = await Promise.all([import("./notifications_CY-v-Hbg.mjs"), import("./client_DbLECgB2.mjs")]);
    lineLinked = (await linkedLineMembers(env).catch(() => [])).length;
    const token = await getApiKey2(env, "line_token").catch(() => null);
    if (token) {
      const [{ cfEgressGateway }, { lineQuota }] = await Promise.all([import("./ctx_DH8R7Lvm.mjs").then((n) => n.M), import("./ctx_DH8R7Lvm.mjs").then((n) => n.R)]);
      lineQuotaInfo = await lineQuota(cfEgressGateway(env), token).catch(() => null);
    }
  }
  return renderTemplate`${renderComponent($$result, "App", $$App, { "title": "外部メッセージ連携", "active": "/settings", "denseMobile": true, "data-astro-cid-xrxid6ce": true }, { "default": async ($$result2) => renderTemplate(_a || (_a = __template([" ", '<h1 data-astro-cid-xrxid6ce>外部メッセージ連携</h1> <p class="muted" data-astro-cid-xrxid6ce>LINE/Discord/Slack で「話しかけて応答（受信）」と「完了通知などを送る（通知）」を、サービスごとに1か所で設定します（Proプラン）。発話者は各サービスのログイン連携で参加した登録メンバーに限定されます。送受信は統制された統一経路（許可リスト＋監査）を通ります。</p> <p class="muted small" data-astro-cid-xrxid6ce>入力中のラベル等はこの端末に一時保存され、リロードしても復元されます（保存すると消えます）。<strong data-astro-cid-xrxid6ce>APIキー・トークン等の機密情報は保存しません</strong>（毎回入力が必要です）。</p> <div class="card" style="border-color:var(--brand-tint);margin-bottom:1rem" data-astro-cid-xrxid6ce> <h2 style="margin:.1rem 0 .3rem" data-astro-cid-xrxid6ce>かんたん通知（おすすめ）</h2> <p class="muted small" style="margin:0 0 .6rem" data-astro-cid-xrxid6ce><strong data-astro-cid-xrxid6ce>承認のお願い・AIの作業完了・期日リマインド</strong>などを <strong data-astro-cid-xrxid6ce>1つのチャンネルにまとめて通知</strong>します。<strong data-astro-cid-xrxid6ce>Discord か Slack の Webhook URL を1つ貼るだけ</strong>。個人のログイン連携や Bot 設定は不要です。</p> <div class="field" data-astro-cid-xrxid6ce><label for="nf-url" data-astro-cid-xrxid6ce>通知用 Webhook URL <span class="muted" data-astro-cid-xrxid6ce>（', '）</span></label><input id="nf-url" type="password" placeholder="https://discord.com/api/webhooks/... または https://hooks.slack.com/services/..." autocomplete="off" data-astro-cid-xrxid6ce></div> <div class="row" style="display:flex;gap:.5rem;flex-wrap:wrap" data-astro-cid-xrxid6ce><button class="btn btn-primary" id="nf-save" type="button" data-astro-cid-xrxid6ce>保存</button><button class="btn" id="nf-test" type="button" data-astro-cid-xrxid6ce>テスト送信</button>', '</div> <details class="setup-guide"', ' style="margin-top:.6rem" data-astro-cid-xrxid6ce><summary data-astro-cid-xrxid6ce><strong data-astro-cid-xrxid6ce>Webhook URL の取り方（約30秒・Discord/Slack）</strong></summary> <div class="wh-guide" data-astro-cid-xrxid6ce> <div class="wh-col" data-astro-cid-xrxid6ce> <p class="wh-h" data-astro-cid-xrxid6ce>🟣 Discord の場合</p> <ol class="wh-steps" data-astro-cid-xrxid6ce> <li data-astro-cid-xrxid6ce>通知したいチャンネルの <strong data-astro-cid-xrxid6ce>⚙ 設定</strong> を開く</li> <li data-astro-cid-xrxid6ce><strong data-astro-cid-xrxid6ce>連携サービス → ウェブフック → 新しいウェブフック</strong> を作成</li> <li data-astro-cid-xrxid6ce><strong data-astro-cid-xrxid6ce>「ウェブフックURLをコピー」</strong> をクリック</li> <li data-astro-cid-xrxid6ce>上の欄に貼り付けて <strong data-astro-cid-xrxid6ce>保存</strong> → <strong data-astro-cid-xrxid6ce>テスト送信</strong></li> </ol> </div> <div class="wh-col" data-astro-cid-xrxid6ce> <p class="wh-h" data-astro-cid-xrxid6ce>🟢 Slack の場合</p> <ol class="wh-steps" data-astro-cid-xrxid6ce> <li data-astro-cid-xrxid6ce><a href="https://api.slack.com/apps" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>Slack: Your Apps ↗</a> を開く</li> <li data-astro-cid-xrxid6ce>対象アプリ → <strong data-astro-cid-xrxid6ce>Incoming Webhooks</strong> を On</li> <li data-astro-cid-xrxid6ce><strong data-astro-cid-xrxid6ce>Add New Webhook</strong> → 通知先チャンネルを選んで許可</li> <li data-astro-cid-xrxid6ce>表示された <strong data-astro-cid-xrxid6ce>URLをコピー</strong> → 上の欄に貼って <strong data-astro-cid-xrxid6ce>保存</strong></li> </ol> </div> </div> </details>  <div style="border-top:1px solid var(--line);margin-top:.8rem;padding-top:.7rem" data-astro-cid-xrxid6ce> <p style="margin:0 0 .3rem;font-weight:600" data-astro-cid-xrxid6ce>LINE にも通知する <span class="muted" style="font-weight:400" data-astro-cid-xrxid6ce>（関係する各メンバー個人へ個別に届く）</span></p> ', ' </div> </div>  <div class="msg-tabs" role="tablist" aria-label="連携サービスの切替" data-astro-cid-xrxid6ce> <button type="button" class="msg-tab on" role="tab" data-svc="line" aria-selected="true" data-astro-cid-xrxid6ce>LINE <span', ' aria-hidden="true" data-astro-cid-xrxid6ce></span></button> <button type="button" class="msg-tab" role="tab" data-svc="discord" aria-selected="false" data-astro-cid-xrxid6ce>Discord <span', ' aria-hidden="true" data-astro-cid-xrxid6ce></span></button> <button type="button" class="msg-tab" role="tab" data-svc="slack" aria-selected="false" data-astro-cid-xrxid6ce>Slack <span', ' aria-hidden="true" data-astro-cid-xrxid6ce></span></button> </div>  <div class="card msg-panel" data-svc="discord" role="tabpanel" hidden data-astro-cid-xrxid6ce> <h2 style="margin:.1rem 0 .6rem" data-astro-cid-xrxid6ce>Discord</h2> <h3 class="sec" data-astro-cid-xrxid6ce>受信（話しかけ → 応答）— 上から順に進めてください</h3> <p class="muted small dc-prereq" data-astro-cid-xrxid6ce>Discord アカウントと、Bot を入れる <strong data-astro-cid-xrxid6ce>サーバー</strong>（あなたが<strong data-astro-cid-xrxid6ce>管理権限</strong>を持つもの）が必要です。まだ無い方は STEP 1・STEP 2 で作成し、すでにお持ちの方はスキップして <strong data-astro-cid-xrxid6ce>STEP 3</strong> から進めてください。</p> <!-- STEP 1（アカウント作成・既存ならスキップ） --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 1</span> Discord アカウントを作る<span class="dc-skip" data-astro-cid-xrxid6ce>お持ちの方はスキップ</span></p> <p class="muted small" data-astro-cid-xrxid6ce>アカウントが無ければ、下のボタンから無料で登録します（メールアドレス・パスワードを設定し、届いたメールで認証）。<strong data-astro-cid-xrxid6ce>すでにお持ちの方はこの STEP を飛ばして STEP 2 へ。</strong></p> <a class="btn btn-primary" href="https://discord.com/register" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>Discord に登録する</a> <p class="dc-next" data-astro-cid-xrxid6ce>✅ アカウントにログインできたら STEP 2 へ</p> </div> <!-- STEP 2（サーバー作成・既存ならスキップ） --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 2</span> サーバーを作る<span class="dc-skip" data-astro-cid-xrxid6ce>お持ちの方はスキップ</span></p> <p class="muted small" data-astro-cid-xrxid6ce>Bot を招待する先のサーバーを用意します。Discord アプリ（ブラウザ／PC／スマホ）左側の <b data-astro-cid-xrxid6ce>＋</b> →「<b data-astro-cid-xrxid6ce>オリジナルの作成</b>」→「<b data-astro-cid-xrxid6ce>自分と友達のため</b>」等を選び、サーバー名（団体名など）を付けて作成します。<strong data-astro-cid-xrxid6ce>すでに管理権限を持つサーバーがあればスキップして STEP 3 へ。</strong></p> <a class="btn btn-primary" href="https://discord.com/channels/@me" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>Discord を開く（サーバー作成）</a> <p class="dc-next" data-astro-cid-xrxid6ce>✅ 管理権限を持つサーバーを用意できたら STEP 3 へ</p> </div> <!-- STEP 3 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 3</span> Discord でアプリと Bot を作る</p> <p class="muted small" data-astro-cid-xrxid6ce>下のボタンで Discord 開発者ページを開き、<b data-astro-cid-xrxid6ce>New Application</b>（名前は自由）→ 左メニュー <b data-astro-cid-xrxid6ce>Bot</b> → <b data-astro-cid-xrxid6ce>Reset Token</b> で表示された <b data-astro-cid-xrxid6ce>Bot Token</b> をコピーします。</p> <a class="btn btn-primary" href="https://discord.com/developers/applications" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>Discord 開発者ページを開く</a> <p class="dc-next" data-astro-cid-xrxid6ce>✅ Bot Token をコピーできたら STEP 4 へ</p> </div> <!-- STEP 4 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 4</span> Bot Token を貼って自動セットアップ', '</p> <p class="muted small" data-astro-cid-xrxid6ce>App ID・Public Key の取得、受信URLの設定、コマンド登録まで自動で行います。</p> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>Discord Bot Token</label><input id="dc-token" type="password" autocomplete="off" placeholder="Bot Token を貼り付け" data-astro-cid-xrxid6ce></div> <button class="btn btn-primary" id="dc-setup" data-astro-cid-xrxid6ce>自動セットアップを実行</button> <div id="dc-result" class="small" style="margin-top:.6rem" data-astro-cid-xrxid6ce></div> <p class="dc-next" data-astro-cid-xrxid6ce>✅「セットアップ完了」と表示されたら STEP 5 へ</p> </div> <!-- STEP 5 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 5</span> Bot をサーバーに招待（必要な権限のみ）</p> <p class="muted small" data-astro-cid-xrxid6ce>下のボタンで招待ページを開き、対象サーバーを選んで認可します。要求するのは<strong data-astro-cid-xrxid6ce>メッセージ送信・履歴閲覧・ピン留め・閲覧・埋め込み/添付</strong>に必要な権限のみで、Administrator（管理者）権限は要求しません。用途に合わせて選べます（§8）。</p> ', ' <p class="dc-next" data-astro-cid-xrxid6ce>✅ サーバーに Bot が追加されたら STEP 6 へ</p> </div> <!-- STEP 6 --> <div class="dc-step" id="dc-panel-details" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 6</span> 「メッセージ送信」ボタンを常設</p> <p class="muted small" data-astro-cid-xrxid6ce><b data-astro-cid-xrxid6ce>baku-office</b> チャンネルを自動作成し、常設ボタンを投稿・ピン留めします（既にあれば再利用）。メンバーはコマンドを打たずボタンから送れます。</p> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>サーバー</label><select id="dc-panel-guild"', ' data-astro-cid-xrxid6ce><option value="" data-astro-cid-xrxid6ce>', '</option></select></div> <button class="btn btn-primary" id="dc-panel-create" data-astro-cid-xrxid6ce>baku-office チャンネルを作成して設置</button> <div id="dc-panel-result" class="small" style="margin-top:.5rem" data-astro-cid-xrxid6ce></div> <details class="setup-guide" style="margin-top:.6rem" data-astro-cid-xrxid6ce><summary data-astro-cid-xrxid6ce>既存のチャンネルに設置する</summary> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>チャンネル</label><select id="dc-panel-channel" data-astro-cid-xrxid6ce><option value="" data-astro-cid-xrxid6ce>サーバーを選択</option></select></div> <button class="btn" id="dc-panel-set" data-astro-cid-xrxid6ce>このチャンネルに設置</button> </details> <p class="dc-next" data-astro-cid-xrxid6ce>✅「パネルを設置しました」と表示されたら STEP 7 へ</p> </div> <!-- STEP 7 --> <div class="dc-step" id="dc-link-details" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 7</span> 自分の Discord を連携（誰が送ったか分かるように）</p> <p class="muted small" data-astro-cid-xrxid6ce>あなた（管理者）の Discord と自分の会員アカウントを紐付けます。連携後は招待コードなしですぐ使えます。</p> <p class="dc-sub" data-astro-cid-xrxid6ce>7-1. 開発者ページの <b data-astro-cid-xrxid6ce>OAuth2 → Redirects</b> に下のURLを追加して保存</p> <div class="copy-row" data-astro-cid-xrxid6ce><code id="dc-redirect-url" data-astro-cid-xrxid6ce>', '/api/discord/link/callback</code><button class="btn btn-sm" data-copy="#dc-redirect-url" data-astro-cid-xrxid6ce>コピー</button></div> <a class="btn" href="https://discord.com/developers/applications" target="_blank" rel="noopener" style="margin-top:.4rem" data-astro-cid-xrxid6ce>開発者ページ（OAuth2）を開く</a> <p class="dc-sub" data-astro-cid-xrxid6ce>7-2. 同じ OAuth2 画面の <b data-astro-cid-xrxid6ce>Client Secret</b>（Reset Secret で表示）を貼って保存</p> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>Discord OAuth2 Client Secret <span class="muted" data-astro-cid-xrxid6ce>（', '）</span></label><input name="discord_client_secret" type="password" autocomplete="off" placeholder="Client Secret を貼り付け" data-astro-cid-xrxid6ce></div> <button class="btn" id="dc-link-save" data-astro-cid-xrxid6ce>Client Secret を保存</button> <p class="dc-sub" data-astro-cid-xrxid6ce>7-3. 連携を実行</p> <a class="btn btn-primary" href="/api/discord/link/start" data-astro-cid-xrxid6ce>自分の Discord を連携</a> <div id="dc-link-result" class="small" style="margin-top:.5rem" data-astro-cid-xrxid6ce></div> <p class="dc-next" data-astro-cid-xrxid6ce>✅「自分の Discord を連携しました」と表示されたら STEP 8 へ</p> </div> <!-- STEP 8 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 8</span> メンバーを招待する</p> <p class="muted small" data-astro-cid-xrxid6ce>他のメンバーが使えるようにするには、招待コードを発行し、各メンバーに Discord で <code data-astro-cid-xrxid6ce>/参加 コード</code> を入力してもらいます。メンバーの参加申請は承認後に有効になります。</p> <a class="btn btn-primary" href="/settings/members" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>メンバー管理を開く（コード発行・承認）</a> <p class="muted small" style="margin:.5rem 0 0" data-astro-cid-xrxid6ce>ボタンが表示されない環境でも、<code data-astro-cid-xrxid6ce>/ask</code> コマンドで送れます。<strong data-astro-cid-xrxid6ce>連携と承認が完了した段階で利用できます</strong>。</p> </div> <details class="setup-guide" style="margin-top:.8rem" data-astro-cid-xrxid6ce><summary data-astro-cid-xrxid6ce>手動で設定（自動が使えない場合）</summary> <p class="muted small" data-astro-cid-xrxid6ce>用途の説明を先に、技術名は補足として表示します。<b data-astro-cid-xrxid6ce>「秘密」</b>の項目は他人に渡さないでください。</p> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>アプリの識別子 <span class="muted" data-astro-cid-xrxid6ce>— Discord Application ID（公開値）（', '）</span></label><input name="discord_app_id" type="password" autocomplete="off" data-astro-cid-xrxid6ce></div> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>受信の署名検証に使う公開鍵 <span class="muted" data-astro-cid-xrxid6ce>— Discord Public Key（公開値）（', '）</span></label><input name="discord_public_key" type="password" autocomplete="off" data-astro-cid-xrxid6ce></div> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>Bot の認証トークン <span class="muted" data-astro-cid-xrxid6ce>— Discord Bot Token（秘密・他人に渡さない）（', '）</span></label><input name="discord_bot_token" type="password" autocomplete="off" data-astro-cid-xrxid6ce></div> <p class="muted small" data-astro-cid-xrxid6ce>受信URL（Interactions Endpoint URL）に設定する値：</p> <div class="copy-row" data-astro-cid-xrxid6ce><code id="dc-inbound-url" data-astro-cid-xrxid6ce>', '/api/inbound/discord</code><button class="btn btn-sm" data-copy="#dc-inbound-url" data-astro-cid-xrxid6ce>コピー</button></div> <div class="row" style="display:flex;gap:.5rem;flex-wrap:wrap;margin-top:.5rem" data-astro-cid-xrxid6ce><button class="btn" id="save-keys" data-astro-cid-xrxid6ce>保存</button><button class="btn" id="register-discord" data-astro-cid-xrxid6ce>コマンドを登録</button></div> </details> <p class="muted small" style="margin-top:.8rem" data-astro-cid-xrxid6ce>通知（お知らせの送信先）は、画面上部の <strong data-astro-cid-xrxid6ce>「かんたん通知」</strong> にまとめました。Discord/Slack の Webhook URL を1本貼るだけで承認・完了などが届きます。</p> </div>  <div class="card msg-panel" data-svc="slack" role="tabpanel" hidden data-astro-cid-xrxid6ce> <h2 style="margin:.1rem 0 .6rem" data-astro-cid-xrxid6ce>Slack</h2> <h3 class="sec" data-astro-cid-xrxid6ce>受信（話しかけ → 応答）</h3> <details class="setup-guide" data-astro-cid-xrxid6ce><summary data-astro-cid-xrxid6ce>① アプリ作成と取得手順</summary> <ol class="guide-steps" data-astro-cid-xrxid6ce> <li data-astro-cid-xrxid6ce><a href="https://api.slack.com/apps" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>Slack API: Your Apps↗</a> で <strong data-astro-cid-xrxid6ce>Create New App → From scratch</strong>。</li> <li data-astro-cid-xrxid6ce><strong data-astro-cid-xrxid6ce>Basic Information</strong> の <strong data-astro-cid-xrxid6ce>Signing Secret</strong> をコピー。</li> <li data-astro-cid-xrxid6ce><strong data-astro-cid-xrxid6ce>OAuth & Permissions</strong> で Bot Token Scopes に <code data-astro-cid-xrxid6ce>app_mentions:read</code>・<code data-astro-cid-xrxid6ce>im:history</code>・<code data-astro-cid-xrxid6ce>chat:write</code> を追加 → <strong data-astro-cid-xrxid6ce>Install to Workspace</strong> → <strong data-astro-cid-xrxid6ce>Bot User OAuth Token（xoxb-…）</strong>をコピー。</li> </ol> </details> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>受信の署名検証に使う秘密 <span class="muted" data-astro-cid-xrxid6ce>— Slack Signing Secret（秘密・他人に渡さない）（', '）</span></label><input id="sl-secret" type="password" autocomplete="off" data-astro-cid-xrxid6ce></div> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>メッセージ送受信に使うトークン <span class="muted" data-astro-cid-xrxid6ce>— Slack Bot Token（秘密・xoxb-…）（', '）</span></label><input id="sl-token" type="password" placeholder="xoxb-..." autocomplete="off" data-astro-cid-xrxid6ce></div> <button class="btn btn-primary" id="slack-setup" data-astro-cid-xrxid6ce>② 検証して保存</button> <div id="sl-result" class="small" style="margin-top:.6rem" data-astro-cid-xrxid6ce></div> <p class="muted small" style="margin-top:.5rem" data-astro-cid-xrxid6ce>保存後、<strong data-astro-cid-xrxid6ce>Event Subscriptions</strong> を有効化し Request URL に <code data-astro-cid-xrxid6ce>', '/api/inbound/slack</code> を設定、bot events に <code data-astro-cid-xrxid6ce>app_mention</code>・<code data-astro-cid-xrxid6ce>message.im</code> を購読。OAuthログインは別途 <code data-astro-cid-xrxid6ce>SLACK_CLIENT_ID/SECRET</code>（Worker Secret）。</p> <p class="muted small" style="margin-top:.8rem" data-astro-cid-xrxid6ce>通知（お知らせの送信先）は、画面上部の <strong data-astro-cid-xrxid6ce>「かんたん通知」</strong> にまとめました。Discord/Slack の Webhook URL を1本貼るだけで承認・完了などが届きます。</p> </div>  <div class="card msg-panel" data-svc="line" role="tabpanel" data-astro-cid-xrxid6ce> <h2 style="margin:.1rem 0 .6rem" data-astro-cid-xrxid6ce>LINE</h2> <h3 class="sec" data-astro-cid-xrxid6ce>受信（話しかけ → 応答）— 上から順に進めてください</h3> <p class="muted small" data-astro-cid-xrxid6ce>対応は <strong data-astro-cid-xrxid6ce>1対1トーク</strong>のみです（利用者は「<code data-astro-cid-xrxid6ce>参加 招待コード</code>」と送ると組織に紐付きます）。グループ/ルーム内の会話には未対応（グループは通知の送信先としてのみ利用できます）。</p> <!-- STEP 1（公式アカウント作成・既存ならスキップ） --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 1</span> LINE公式アカウントを作る<span class="dc-skip" data-astro-cid-xrxid6ce>お持ちの方はスキップ</span></p> <p class="muted small" data-astro-cid-xrxid6ce>下のボタンで <strong data-astro-cid-xrxid6ce>LINE Official Account Manager</strong> を開き、<strong data-astro-cid-xrxid6ce>LINEビジネスID</strong>（メールアドレス、または普段の LINE アカウントで登録）でログイン →「<strong data-astro-cid-xrxid6ce>作成</strong>」から <strong data-astro-cid-xrxid6ce>LINE公式アカウント</strong>（団体名など）を作成します。<strong data-astro-cid-xrxid6ce>すでにお持ちの方はスキップして STEP 2 へ。</strong></p> <a class="btn btn-primary" href="https://manager.line.biz/" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>LINE公式アカウントを作成</a> <p class="dc-next" data-astro-cid-xrxid6ce>✅ 公式アカウントを作成できたら STEP 2 へ</p> </div> <!-- STEP 2 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 2</span> Messaging API を有効化する</p> <p class="muted small" data-astro-cid-xrxid6ce>同じ <strong data-astro-cid-xrxid6ce>LINE Official Account Manager</strong> で対象アカウントを開き、「<strong data-astro-cid-xrxid6ce>設定</strong>」→「<strong data-astro-cid-xrxid6ce>Messaging API</strong>」→「<strong data-astro-cid-xrxid6ce>Messaging APIを利用する</strong>」を選択。<strong data-astro-cid-xrxid6ce>プロバイダー</strong>（団体名など。無ければ新規作成）を選ぶと、<strong data-astro-cid-xrxid6ce>Messaging API チャネル</strong>が作られます。</p> <a class="btn btn-primary" href="https://manager.line.biz/" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>LINE Official Account Manager を開く</a> <p class="dc-next" data-astro-cid-xrxid6ce>✅「Messaging APIを利用する」を完了したら STEP 3 へ</p> </div> <!-- STEP 3 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 3</span> LINE Developers にログインする</p> <p class="muted small" data-astro-cid-xrxid6ce>下のボタンで <strong data-astro-cid-xrxid6ce>LINE Developers</strong> を開き、<strong data-astro-cid-xrxid6ce>STEP 1 と同じ LINEビジネスID</strong> でログインします。STEP 2 で作成した <strong data-astro-cid-xrxid6ce>プロバイダー</strong>と <strong data-astro-cid-xrxid6ce>Messaging API チャネル</strong>が表示されます。</p> <a class="btn btn-primary" href="https://developers.line.biz/console/" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>LINE Developers を開く</a> <p class="dc-next" data-astro-cid-xrxid6ce>✅ ログインしてチャネルを開けたら STEP 4 へ</p> </div> <!-- STEP 4 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 4</span> シークレットとアクセストークンを発行してコピーする</p> <p class="muted small" data-astro-cid-xrxid6ce>LINE Developers の対象チャネルで、<strong data-astro-cid-xrxid6ce>「チャネル基本設定」</strong>の <strong data-astro-cid-xrxid6ce>チャネルシークレット</strong>をコピー、<strong data-astro-cid-xrxid6ce>「Messaging API設定」</strong>の <strong data-astro-cid-xrxid6ce>チャネルアクセストークン（長期）</strong>を「発行」してコピーします（次の STEP で貼り付けます）。</p> <p class="dc-next" data-astro-cid-xrxid6ce>✅ シークレットとトークンをコピーできたら STEP 5 へ</p> </div> <!-- STEP 5 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 5</span> 貼り付けて自動セットアップ</p> <p class="muted small" data-astro-cid-xrxid6ce>保存＋Webhook URL の設定まで自動で行います（コンソールでの URL 設定は不要）。</p> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>受信の署名検証に使う秘密鍵 <span class="muted" data-astro-cid-xrxid6ce>— LINE チャネルシークレット（秘密・他人に渡さない）（', '）</span></label><input id="li-secret" type="password" autocomplete="off" data-astro-cid-xrxid6ce></div> <div class="field" data-astro-cid-xrxid6ce><label data-astro-cid-xrxid6ce>メッセージ送受信に使うトークン <span class="muted" data-astro-cid-xrxid6ce>— LINE チャネルアクセストークン（秘密・他人に渡さない）（', '）</span></label><input id="li-token" type="password" autocomplete="off" placeholder="先頭に余分な文字を入れない" data-astro-cid-xrxid6ce></div> <button class="btn btn-primary" id="line-setup" data-astro-cid-xrxid6ce>自動セットアップを実行</button> <div id="li-result" class="small" style="margin-top:.6rem" data-astro-cid-xrxid6ce></div> <p class="dc-next" data-astro-cid-xrxid6ce>✅「確認待ち」と表示されたら、自分の LINE で公式アカウントを友だち追加し「<code data-astro-cid-xrxid6ce>参加 招待コード</code>」を送って応答を確認してください</p> </div> <!-- STEP 6 --> <div class="dc-step" data-astro-cid-xrxid6ce> <p class="dc-h" data-astro-cid-xrxid6ce><span class="dc-n" data-astro-cid-xrxid6ce>STEP 6</span> メンバーを招待する</p> <p class="muted small" data-astro-cid-xrxid6ce>メンバーは公式アカウントを友だち追加し、トークで「<code data-astro-cid-xrxid6ce>参加 招待コード</code>」（例：<code data-astro-cid-xrxid6ce>参加 ABC123</code>）と送ると組織に紐付きます。招待コードは<a href="/settings/members" target="_blank" rel="noopener" data-astro-cid-xrxid6ce>メンバー管理</a>で発行・承認できます。<strong data-astro-cid-xrxid6ce>連携と承認が完了した段階で利用できます</strong>。</p> </div> <p class="muted small" style="margin-top:.8rem" data-astro-cid-xrxid6ce>LINE への通知（お知らせ）は、画面上部の <strong data-astro-cid-xrxid6ce>「かんたん通知」→「LINEにも通知する」</strong> にまとめました。公式アカウントに話しかけて1タップで通知先を設定できます。</p> </div> <script type="application/json" id="conn-labels">', "<\/script>  "])), maybeRenderHead(), notifyWebhookSet ? "設定済み・変更する場合のみ入力" : "未設定", notifyWebhookSet && renderTemplate`<button class="btn btn-danger" id="nf-clear" type="button" data-astro-cid-xrxid6ce>解除</button>`, addAttribute(!notifyWebhookSet, "open"), status.line_token ? renderTemplate`${renderComponent($$result2, "Fragment", Fragment, {}, { "default": async ($$result3) => renderTemplate` <p class="muted small" style="margin:0 0 .5rem" data-astro-cid-xrxid6ce>設定は不要です。<strong data-astro-cid-xrxid6ce>各メンバーが LINE 公式アカウントに「参加 &lt;招待コード&gt;」と送って連携</strong>すると、以後<strong data-astro-cid-xrxid6ce>その本人に関係する通知だけ</strong>が自分のLINEへ届きます（自分が依頼したAI作業の完了 等。承認のお願いは管理者へ）。<strong data-astro-cid-xrxid6ce>全員への一斉配信ではありません</strong>。招待コードは<a href="/settings/members" data-astro-cid-xrxid6ce>メンバー管理</a>で発行できます。</p> <p class="muted small" style="margin:.2rem 0 .5rem" data-astro-cid-xrxid6ce>
連携済みメンバー：<strong data-astro-cid-xrxid6ce>${lineLinked}人</strong> ${lineQuotaInfo && (lineQuotaInfo.limited ? renderTemplate`<span data-astro-cid-xrxid6ce> ／　今月の通知（push）：<strong data-astro-cid-xrxid6ce>${lineQuotaInfo.used} / ${lineQuotaInfo.limit} 通</strong>（reply は消費しません）</span>` : renderTemplate`<span data-astro-cid-xrxid6ce> ／　今月の通知（push）：使用 <strong data-astro-cid-xrxid6ce>${lineQuotaInfo.used} 通</strong>（上限なしプラン）</span>`)} </p> <div class="row" style="display:flex;gap:.5rem;flex-wrap:wrap" data-astro-cid-xrxid6ce> <button class="btn" id="nl-test" type="button" data-astro-cid-xrxid6ce>自分のLINEへテスト送信</button> </div> <div id="nl-result" class="muted small" style="margin-top:.4rem" data-astro-cid-xrxid6ce></div> ${lineLinked === 0 && renderTemplate`<p class="muted small" style="margin:.4rem 0 0;color:var(--danger)" data-astro-cid-xrxid6ce>まだ連携済みメンバーがいません。メンバーに「LINE公式アカウントを友だち追加→『参加 招待コード』と送信」を案内してください（あなた自身も連携すると上のテストが届きます）。</p>`}` })}` : renderTemplate`<p class="muted small" style="margin:0" data-astro-cid-xrxid6ce>LINE に通知するには、まず下の <button type="button" class="btn btn-sm" id="nl-goto" data-astro-cid-xrxid6ce>「LINE」タブで受信設定</button> を済ませてください（Botトークンの登録）。その後、連携した各メンバーへ個別に通知が届きます。</p>`, addAttribute("msg-dot" + (svcReady.line ? " on" : ""), "class"), addAttribute("msg-dot" + (svcReady.discord ? " on" : ""), "class"), addAttribute("msg-dot" + (svcReady.slack ? " on" : ""), "class"), status.discord_bot_token && renderTemplate`<span class="dc-set" data-astro-cid-xrxid6ce>✓ 設定済み</span>`, dcInviteUrl ? renderTemplate`${renderComponent($$result2, "Fragment", Fragment, {}, { "default": async ($$result3) => renderTemplate` <div class="dc-invite-opts" data-astro-cid-xrxid6ce> <a class="btn btn-primary" id="dc-invite-btn"${addAttribute(dcInviteUrl, "href")} target="_blank" rel="noopener" data-astro-cid-xrxid6ce>招待（baku-office チャンネルを自動作成）</a> <a class="btn" id="dc-invite-btn-min"${addAttribute(dcInviteUrlNoCreate, "href")} target="_blank" rel="noopener" data-astro-cid-xrxid6ce>招待（既存チャンネルだけで使う・作成権限なし）</a> </div> <p class="muted small" style="margin:.4rem 0 0" data-astro-cid-xrxid6ce>「自動作成」は STEP 4 で <code data-astro-cid-xrxid6ce>baku-office</code> チャンネルを作る場合に。既存チャンネルへ設置するだけなら、より権限の少ない右のボタンで十分です（<strong data-astro-cid-xrxid6ce>チャンネル作成（管理）権限を要求しません</strong>）。</p> ` })}` : renderTemplate`<button class="btn" id="dc-invite-btn" disabled data-astro-cid-xrxid6ce>先に STEP 4（自動セットアップ）を完了してください</button>`, addAttribute(status.discord_bot_token ? "1" : "", "data-ready"), status.discord_bot_token ? "読み込み中…" : "STEP 4（自動セットアップ）を完了すると一覧を取得します", O, mark(status.discord_client_secret), mark(status.discord_app_id), mark(status.discord_public_key), mark(status.discord_bot_token), O, mark(status.slack_signing_secret), mark(status.slack_bot_token), O, mark(status.line_secret), mark(status.line_token), unescapeHTML(JSON.stringify(connLabels))), "scripts": async ($$result2) => renderTemplate(_b || (_b = __template(['<script data-astro-rerun>\n    // SPA遷移(ClientRouter)で data-astro-rerun により本スクリプトは再実行される。トップレベル const のまま\n    // だと再実行時に「重複宣言」で全体が失敗し、タブ等が動かなくなる。IIFEで包んで毎回新スコープにする。\n    (function () {\n    // かんたん通知（通知用 Webhook URL）：保存／テスト送信／解除。\n    document.getElementById("nf-save")?.addEventListener("click", async (e) => {\n      const u = (document.getElementById("nf-url")?.value || "").trim();\n      if (!/^https:\\/\\/(discord\\.com|discordapp\\.com|hooks\\.slack\\.com)\\//.test(u)) { window.bo.toast("Discord か Slack の Webhook URL を貼ってください", "err"); return; }\n      const r = await window.bo.api("/api/settings", { _action: "notify_webhook", webhook: u }, { btn: e.currentTarget, successMsg: "保存しました" });\n      if (r.ok) setTimeout(() => location.reload(), 600);\n    });\n    document.getElementById("nf-test")?.addEventListener("click", async (e) => {\n      const u = (document.getElementById("nf-url")?.value || "").trim();\n      const r = await window.bo.api("/api/settings", { _action: "notify_webhook_test", webhook: u }, { btn: e.currentTarget, successMsg: null });\n      window.bo.toast(r.ok && r.data?.sent ? "テスト通知を送信しました。チャンネルを確認してください。" : "送信できませんでした。URLと保存状態を確認してください。", r.ok && r.data?.sent ? "ok" : "err");\n    });\n    document.getElementById("nf-clear")?.addEventListener("click", async (e) => {\n      if (!(await window.bo.confirm("通知用 Webhook を解除しますか？", { confirmLabel: "解除", danger: true }))) return;\n      const r = await window.bo.api("/api/settings", { _action: "notify_webhook", webhook: "" }, { btn: e.currentTarget, successMsg: "解除しました" });\n      if (r.ok) setTimeout(() => location.reload(), 600);\n    });\n    // LINE 通知先：話しかけた送信元を取得して設定／テスト／解除／LINEタブへ移動。\n    document.getElementById("nl-test")?.addEventListener("click", async (e) => {\n      const r = await window.bo.api("/api/settings", { _action: "notify_line_test" }, { btn: e.currentTarget, successMsg: null });\n      const box = document.getElementById("nl-result");\n      const ok = r.ok && r.data?.sent;\n      if (box) box.textContent = ok ? "あなたのLINEへテスト通知を送信しました。ご確認ください。" : "あなた宛に送信できませんでした。ご自身が LINE連携（公式アカウントに『参加 招待コード』）済みか、Botトークンの登録をご確認ください。";\n      window.bo.toast(ok ? "テスト通知を送信しました" : "未連携の可能性があります", ok ? "ok" : "err");\n    });\n    document.getElementById("nl-goto")?.addEventListener("click", () => { const t = document.querySelector(\'.msg-tab[data-svc="line"]\'); if (t) t.click(); document.querySelector(\'[data-svc="line"].msg-panel\')?.scrollIntoView({ behavior: "smooth" }); });\n    const esc = (s) => String(s).replace(/[&<>"]/g, (m) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", \'"\': "&quot;" }[m]));\n    const val = (id) => (document.getElementById(id)?.value || "").trim();\n    // 状態語彙の共通出典（conn-status.connLabel）をサーバから受け取り、セットアップ結果でも同じ言葉を使う。\n    const CL = (() => { try { return JSON.parse(document.getElementById("conn-labels")?.textContent || "{}"); } catch { return {}; } })();\n\n    // フォーム下書きの一時キャッシュ（data-draft の非機密 input のみ）。機密（シークレット/トークン/宛先=password）は対象外。\n    const DRAFT_KEY = "bo_draft_messaging";\n    const draftEls = () => Array.prototype.slice.call(document.querySelectorAll("input[data-draft]"));\n    const saveDraft = () => { try { const o = {}; draftEls().forEach((el) => { if (el.value) o[el.id] = el.value; }); localStorage.setItem(DRAFT_KEY, JSON.stringify(o)); } catch (e) { /* 容量超過等は無視 */ } };\n    (function loadDraft() {\n      let o = null; try { o = JSON.parse(localStorage.getItem(DRAFT_KEY) || "null"); } catch (e) { o = null; }\n      if (o) draftEls().forEach((el) => { if (o[el.id] != null && o[el.id] !== "") el.value = o[el.id]; });\n      draftEls().forEach((el) => el.addEventListener("input", saveDraft));\n    })();\n\n    // §13：サービス切替タブ。Discord/Slack/LINE を1つずつ表示（スマホで縦長を解消）。URLハッシュ(#slack 等)で深リンク。\n    (function msgTabs() {\n      const tabs = Array.prototype.slice.call(document.querySelectorAll(".msg-tab"));\n      const panels = Array.prototype.slice.call(document.querySelectorAll(".msg-panel"));\n      if (!tabs.length) return;\n      const show = (svc) => {\n        tabs.forEach((t) => { const on = t.dataset.svc === svc; t.classList.toggle("on", on); t.setAttribute("aria-selected", on ? "true" : "false"); });\n        panels.forEach((p) => { p.hidden = p.dataset.svc !== svc; });\n      };\n      tabs.forEach((t) => t.addEventListener("click", () => { show(t.dataset.svc); try { history.replaceState(null, "", "#" + t.dataset.svc); } catch (e) { /* noop */ } }));\n      const init = (location.hash || "").replace("#", "");\n      show(["line", "discord", "slack"].indexOf(init) >= 0 ? init : "line");\n    })();\n\n    // ── 受信セットアップ（Discord / Slack / LINE）\n    document.getElementById("dc-setup")?.addEventListener("click", async (e) => {\n      const token = val("dc-token");\n      if (!token) { window.bo.toast("Bot Token を入力してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/discord/setup", { token }, { btn: e.currentTarget, successMsg: null });\n      const el = document.getElementById("dc-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      const yn = (b) => (b ? "✓" : "✗");\n      // P1-11/P2-07：受信URL設定・コマンド登録が両方成功＝設定完了だが、実際の送受信は未確認なので「確認待ち」。\n      // 片方でも失敗なら「一部のみ利用可」と正直に表示。「接続済み」は実テスト成功時の語彙として温存する。\n      const ready = j.interactionsSet && j.commandsRegistered;\n      if (el) el.innerHTML = `<div>${ready ? `✅ ${CL.pending || "確認待ち"}` : `⚠️ ${CL.warning || "一部のみ利用可"}（下記の✗を解消してください）`}</div>`\n        + `<div>App ID 取得・保存：✓ <code>${esc(j.appId)}</code></div>`\n        + `<div>受信URL設定：${yn(j.interactionsSet)}${j.interactionsSet ? "" : ` <span class="muted">(手動で <code>${esc(j.interactionsUrl)}</code> を設定)</span>`}</div>`\n        + `<div>コマンド登録：${yn(j.commandsRegistered)}${j.commandsRegistered ? "" : ` <span class="muted">(${esc(j.commandsDetail || "再実行してください")})</span>`}</div>`\n        + `<div class="muted" style="margin-top:.3rem">${ready ? "→ STEP 5 で Bot を招待し、実際に話しかけて応答を確認してください。" : "→ 未完了の項目を解消してから STEP 5 へ進んでください。"}</div>`;\n      window.bo.toast(ready ? (CL.pending || "確認待ち") : `${CL.warning || "一部のみ利用可"}（受信URL/コマンド登録を確認）`, ready ? "ok" : "err");\n      // STEP 5 の招待ボタンを有効化（App ID 取得済みになったため）。2系統（自動作成あり/既存チャンネル用）を更新。\n      const inv = document.getElementById("dc-invite-btn");\n      const invMin = document.getElementById("dc-invite-btn-min");\n      if (invMin) {\n        // 既に2系統が描画済み（再セットアップ）＝href のみ更新。\n        if (inv && j.inviteUrl) inv.setAttribute("href", j.inviteUrl);\n        if (j.inviteUrlNoCreate) invMin.setAttribute("href", j.inviteUrlNoCreate);\n      } else if (inv && j.inviteUrl) {\n        // 初回（無効ボタンのみ）→ 2系統を注入。\n        inv.outerHTML = `<div class="dc-invite-opts">`\n          + `<a class="btn btn-primary" id="dc-invite-btn" href="${esc(j.inviteUrl)}" target="_blank" rel="noopener">招待（baku-office チャンネルを自動作成）</a>`\n          + (j.inviteUrlNoCreate ? `<a class="btn" id="dc-invite-btn-min" href="${esc(j.inviteUrlNoCreate)}" target="_blank" rel="noopener">招待（既存チャンネルだけで使う・作成権限なし）</a>` : "")\n          + `</div>`;\n      }\n      document.getElementById("dc-token").value = "";\n    });\n    document.getElementById("register-discord")?.addEventListener("click", async (e) => {\n      const { ok, data: j } = await window.bo.api("/api/discord/register", {}, { btn: e.currentTarget, successMsg: null });\n      window.bo.toast(ok ? (j.message || "登録しました") : (j.error || "登録に失敗しました"), ok ? "ok" : "err");\n    });\n\n    // ── 常設パネル設置（Discord）：サーバー→チャンネルを選びボタンを投稿・ピン留め\n    const gsel = document.getElementById("dc-panel-guild");\n    const csel = document.getElementById("dc-panel-channel");\n    async function loadGuilds() {\n      if (!gsel) return;\n      const r = await fetch("/api/discord/panel", { credentials: "same-origin" });\n      const j = await r.json().catch(() => ({}));\n      if (!r.ok) { gsel.innerHTML = `<option value="">${esc(j.error || "読み込みに失敗")}</option>`; return; }\n      const gs = j.guilds || [];\n      gsel.innerHTML = gs.length\n        ? `<option value="">選択してください</option>` + gs.map((g) => `<option value="${esc(g.id)}">${esc(g.name)}</option>`).join("")\n        : `<option value="">bot が参加しているサーバーがありません（先にサーバーへ追加）</option>`;\n    }\n    async function loadChannels(guildId) {\n      if (!csel) return;\n      if (!guildId) { csel.innerHTML = `<option value="">サーバーを選択</option>`; return; }\n      csel.innerHTML = `<option value="">読み込み中…</option>`;\n      const r = await fetch("/api/discord/panel?guild=" + encodeURIComponent(guildId), { credentials: "same-origin" });\n      const j = await r.json().catch(() => ({}));\n      const cs = (r.ok && j.channels) || [];\n      csel.innerHTML = cs.length\n        ? `<option value="">選択してください</option>` + cs.map((c) => `<option value="${esc(c.id)}">#${esc(c.name)}</option>`).join("")\n        : `<option value="">投稿できるテキストチャンネルがありません</option>`;\n    }\n    gsel?.addEventListener("change", (e) => loadChannels(e.currentTarget.value));\n    // §13：Bot Token 未設定（STEP 4 未完）では panel API が 400 を返すため、初期ロードで呼ばない（不要なコンソール400を防ぐ）。\n    if (gsel && gsel.dataset.ready) loadGuilds();\n    // コピー用ボタン（data-copy="#id" の要素テキストをクリップボードへ）\n    document.querySelectorAll("[data-copy]").forEach((b) => b.addEventListener("click", () => {\n      const t = document.querySelector(b.getAttribute("data-copy"))?.textContent || "";\n      navigator.clipboard.writeText(t).then(() => window.bo.toast("コピーしました", "ok")).catch(() => window.bo.toast("コピーできませんでした", "err"));\n    }));\n    function showPanelResult(ok, j, created) {\n      const el = document.getElementById("dc-panel-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      const pin = j.pinned ? "（ピン留め済み）" : "（ピン留めは権限不足のためスキップ。手動でピン留めしてください）";\n      if (el) el.innerHTML = `✓ ${created ? "baku-office チャンネルを用意し、" : ""}パネルを設置しました${pin}`;\n      window.bo.toast("パネルを設置しました", "ok");\n    }\n    document.getElementById("dc-panel-set")?.addEventListener("click", async (e) => {\n      const channelId = csel?.value;\n      if (!channelId) { window.bo.toast("チャンネルを選択してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/discord/panel", { channelId }, { btn: e.currentTarget, successMsg: null });\n      showPanelResult(ok, j, false);\n    });\n    document.getElementById("dc-panel-create")?.addEventListener("click", async (e) => {\n      const guildId = gsel?.value;\n      if (!guildId) { window.bo.toast("サーバーを選択してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/discord/panel", { guildId, createChannel: true }, { btn: e.currentTarget, successMsg: null });\n      showPanelResult(ok, j, j && j.createdChannel);\n    });\n    // ④ 自分のDiscord連携：Client Secret を保存／コールバック結果(?dc_link=)を表示\n    document.getElementById("dc-link-save")?.addEventListener("click", async (e) => {\n      const inp = document.querySelector(\'input[name="discord_client_secret"]\');\n      if (!inp?.value) { window.bo.toast("Client Secret を入力してください", "err"); return; }\n      await window.bo.api("/api/keys", { discord_client_secret: inp.value }, { btn: e.currentTarget, successMsg: "保存しました" });\n    });\n    (() => {\n      const r = new URLSearchParams(location.search).get("dc_link");\n      if (!r) return;\n      const msg = { ok: "✓ 自分の Discord を連携しました。", dup: "この Discord は別の会員に連携済みです。", state: "連携を中断しました（再度お試しください）。", oauth: "Discord 認可に失敗しました（Client Secret / Redirect URL を確認）。", config: "App ID と Client Secret を先に保存してください。" }[r];\n      const el = document.getElementById("dc-link-result");\n      if (el && msg) { el.textContent = msg; document.getElementById("dc-link-details")?.setAttribute("open", ""); }\n      window.bo.toast(msg || "", r === "ok" ? "ok" : "err");\n    })();\n    document.getElementById("save-keys")?.addEventListener("click", async (e) => {\n      const data = {};\n      document.querySelectorAll("input[name]").forEach((i) => { if (i.value) data[i.name] = i.value; });\n      if (!Object.keys(data).length) { window.bo.toast("変更はありません", "info"); return; }\n      const { ok } = await window.bo.api("/api/keys", data, { btn: e.currentTarget, successMsg: "保存しました" });\n      if (ok) setTimeout(() => location.reload(), 800);\n    });\n    document.getElementById("slack-setup")?.addEventListener("click", async (e) => {\n      const signingSecret = val("sl-secret"), botToken = val("sl-token");\n      if (!signingSecret || !botToken) { window.bo.toast("Signing Secret と Bot Token を入力してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/slack/setup", { signingSecret, botToken }, { btn: e.currentTarget, successMsg: null });\n      const el = document.getElementById("sl-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      if (el) el.innerHTML = `<div>✓ ${CL.saved || "保存済み"}（team: <code>${esc(j.team || "")}</code>）<span class="muted">／受信URLは手動設定が必要</span></div><div class="muted">Event Subscriptions の Request URL に <code>${esc(j.eventsUrl)}</code> を設定してください。</div>`;\n      window.bo.toast(CL.saved || "保存済み", "ok");\n    });\n    document.getElementById("line-setup")?.addEventListener("click", async (e) => {\n      const secret = val("li-secret"), token = val("li-token");\n      if (!secret || !token) { window.bo.toast("シークレットとトークンを入力してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/line/setup", { secret, token }, { btn: e.currentTarget, successMsg: null });\n      const el = document.getElementById("li-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      // P1-09/P2-07：Webhook の「URL設定」と「利用ON」の両方が成功＝設定完了（送受信は未確認なので「確認待ち」）。\n      // 以前は webhookSet のみを見て利用ON（webhookActive）の失敗を無視していた＝ON漏れで受信が無音死する穴があった。\n      const whOk = j.webhookSet && j.webhookActive;\n      if (el) el.innerHTML = `<div>${whOk ? `✅ ${CL.pending || "確認待ち"}` : `⚠️ ${CL.warning || "一部のみ利用可"}`}（${esc(j.botName || j.basicId || "bot")}）</div><div>Webhook URL 設定：${j.webhookSet ? "✓ 自動設定済み" : `✗ 手動で <code>${esc(j.webhookUrl)}</code> を設定${j.webhookDetail ? ` <span class="muted">(${esc(j.webhookDetail)})</span>` : ""}`}</div><div>Webhook 利用：${j.webhookActive ? "✓ ON（自動設定済み）" : `✗ OFF <span class="muted">— LINE Developers コンソールで「Webhook の利用」を ON にしてください${j.webhookActiveDetail ? `（${esc(j.webhookActiveDetail)}）` : ""}</span>`}</div>${whOk ? \'<div class="muted" style="margin-top:.3rem">→ 自分のLINEから「参加 招待コード」を送り、応答を確認してください。</div>\' : ""}`;\n      window.bo.toast(whOk ? (CL.pending || "確認待ち") : `${CL.warning || "一部のみ利用可"}（Webhook設定を確認）`, whOk ? "ok" : "err");\n    });\n\n    // ── 通知（connectors）一覧・追加・削除（プロバイダ別）\n    async function loadConn() {\n      const r = await fetch("/api/connectors", { credentials: "same-origin" });\n      const j = await r.json().catch(() => ({}));\n      const cs = j.connectors || [];\n      for (const prov of ["discord", "slack", "line"]) {\n        const el = document.getElementById("conn-" + prov);\n        if (!el) continue;\n        if (!r.ok) { el.innerHTML = `<p class="muted">${esc(j.error || "読み込みに失敗")}</p>`; continue; }\n        const rows = cs.filter((c) => c.connector === prov);\n        el.innerHTML = rows.length\n          ? \'<table class="tbl"><thead><tr><th>ID</th><th>ラベル</th><th>状態</th><th></th></tr></thead><tbody>\'\n            + rows.map((c) => `<tr><td><code>${esc(c.id)}</code></td><td>${esc(c.label)}</td><td>${c.enabled ? "有効" : "無効"}</td><td><button class="btn btn-sm cdel" data-id="${esc(c.id)}">削除</button></td></tr>`).join("")\n            + "</tbody></table>"\n          : \'<p class="muted">通知先は未登録です。</p>\';\n        el.querySelectorAll(".cdel").forEach((b) => b.addEventListener("click", delConn));\n      }\n    }\n    async function delConn(e) {\n      const id = e.currentTarget.dataset.id;\n      if (!confirm(`「${id}」を削除しますか？`)) return;\n      const { ok, data: j } = await window.bo.api("/api/connectors", { op: "delete", id }, { btn: e.currentTarget, successMsg: null });\n      window.bo.toast(ok ? "削除しました" : (j.error || "削除失敗"), ok ? "ok" : "err");\n      if (ok) loadConn();\n    }\n    for (const prov of ["discord", "slack", "line"]) {\n      document.getElementById("c-add-" + prov)?.addEventListener("click", async (e) => {\n        const id = val("c-id-" + prov), label = val("c-label-" + prov), address = val("c-addr-" + prov);\n        if (!id || !label || !address) { window.bo.toast("用途・ラベル・宛先は必須です", "err"); return; }\n        const { ok, data: j } = await window.bo.api("/api/connectors", { id, label, connector: prov, address }, { btn: e.currentTarget, successMsg: null });\n        window.bo.toast(ok ? "保存しました" : (j.error || "保存失敗"), ok ? "ok" : "err");\n        if (ok) { document.getElementById("c-id-" + prov).value = ""; document.getElementById("c-label-" + prov).value = ""; document.getElementById("c-addr-" + prov).value = ""; saveDraft(); loadConn(); }\n      });\n    }\n    loadConn();\n    })();\n  <\/script>'], ['<script data-astro-rerun>\n    // SPA遷移(ClientRouter)で data-astro-rerun により本スクリプトは再実行される。トップレベル const のまま\n    // だと再実行時に「重複宣言」で全体が失敗し、タブ等が動かなくなる。IIFEで包んで毎回新スコープにする。\n    (function () {\n    // かんたん通知（通知用 Webhook URL）：保存／テスト送信／解除。\n    document.getElementById("nf-save")?.addEventListener("click", async (e) => {\n      const u = (document.getElementById("nf-url")?.value || "").trim();\n      if (!/^https:\\\\/\\\\/(discord\\\\.com|discordapp\\\\.com|hooks\\\\.slack\\\\.com)\\\\//.test(u)) { window.bo.toast("Discord か Slack の Webhook URL を貼ってください", "err"); return; }\n      const r = await window.bo.api("/api/settings", { _action: "notify_webhook", webhook: u }, { btn: e.currentTarget, successMsg: "保存しました" });\n      if (r.ok) setTimeout(() => location.reload(), 600);\n    });\n    document.getElementById("nf-test")?.addEventListener("click", async (e) => {\n      const u = (document.getElementById("nf-url")?.value || "").trim();\n      const r = await window.bo.api("/api/settings", { _action: "notify_webhook_test", webhook: u }, { btn: e.currentTarget, successMsg: null });\n      window.bo.toast(r.ok && r.data?.sent ? "テスト通知を送信しました。チャンネルを確認してください。" : "送信できませんでした。URLと保存状態を確認してください。", r.ok && r.data?.sent ? "ok" : "err");\n    });\n    document.getElementById("nf-clear")?.addEventListener("click", async (e) => {\n      if (!(await window.bo.confirm("通知用 Webhook を解除しますか？", { confirmLabel: "解除", danger: true }))) return;\n      const r = await window.bo.api("/api/settings", { _action: "notify_webhook", webhook: "" }, { btn: e.currentTarget, successMsg: "解除しました" });\n      if (r.ok) setTimeout(() => location.reload(), 600);\n    });\n    // LINE 通知先：話しかけた送信元を取得して設定／テスト／解除／LINEタブへ移動。\n    document.getElementById("nl-test")?.addEventListener("click", async (e) => {\n      const r = await window.bo.api("/api/settings", { _action: "notify_line_test" }, { btn: e.currentTarget, successMsg: null });\n      const box = document.getElementById("nl-result");\n      const ok = r.ok && r.data?.sent;\n      if (box) box.textContent = ok ? "あなたのLINEへテスト通知を送信しました。ご確認ください。" : "あなた宛に送信できませんでした。ご自身が LINE連携（公式アカウントに『参加 招待コード』）済みか、Botトークンの登録をご確認ください。";\n      window.bo.toast(ok ? "テスト通知を送信しました" : "未連携の可能性があります", ok ? "ok" : "err");\n    });\n    document.getElementById("nl-goto")?.addEventListener("click", () => { const t = document.querySelector(\'.msg-tab[data-svc="line"]\'); if (t) t.click(); document.querySelector(\'[data-svc="line"].msg-panel\')?.scrollIntoView({ behavior: "smooth" }); });\n    const esc = (s) => String(s).replace(/[&<>"]/g, (m) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", \'"\': "&quot;" }[m]));\n    const val = (id) => (document.getElementById(id)?.value || "").trim();\n    // 状態語彙の共通出典（conn-status.connLabel）をサーバから受け取り、セットアップ結果でも同じ言葉を使う。\n    const CL = (() => { try { return JSON.parse(document.getElementById("conn-labels")?.textContent || "{}"); } catch { return {}; } })();\n\n    // フォーム下書きの一時キャッシュ（data-draft の非機密 input のみ）。機密（シークレット/トークン/宛先=password）は対象外。\n    const DRAFT_KEY = "bo_draft_messaging";\n    const draftEls = () => Array.prototype.slice.call(document.querySelectorAll("input[data-draft]"));\n    const saveDraft = () => { try { const o = {}; draftEls().forEach((el) => { if (el.value) o[el.id] = el.value; }); localStorage.setItem(DRAFT_KEY, JSON.stringify(o)); } catch (e) { /* 容量超過等は無視 */ } };\n    (function loadDraft() {\n      let o = null; try { o = JSON.parse(localStorage.getItem(DRAFT_KEY) || "null"); } catch (e) { o = null; }\n      if (o) draftEls().forEach((el) => { if (o[el.id] != null && o[el.id] !== "") el.value = o[el.id]; });\n      draftEls().forEach((el) => el.addEventListener("input", saveDraft));\n    })();\n\n    // §13：サービス切替タブ。Discord/Slack/LINE を1つずつ表示（スマホで縦長を解消）。URLハッシュ(#slack 等)で深リンク。\n    (function msgTabs() {\n      const tabs = Array.prototype.slice.call(document.querySelectorAll(".msg-tab"));\n      const panels = Array.prototype.slice.call(document.querySelectorAll(".msg-panel"));\n      if (!tabs.length) return;\n      const show = (svc) => {\n        tabs.forEach((t) => { const on = t.dataset.svc === svc; t.classList.toggle("on", on); t.setAttribute("aria-selected", on ? "true" : "false"); });\n        panels.forEach((p) => { p.hidden = p.dataset.svc !== svc; });\n      };\n      tabs.forEach((t) => t.addEventListener("click", () => { show(t.dataset.svc); try { history.replaceState(null, "", "#" + t.dataset.svc); } catch (e) { /* noop */ } }));\n      const init = (location.hash || "").replace("#", "");\n      show(["line", "discord", "slack"].indexOf(init) >= 0 ? init : "line");\n    })();\n\n    // ── 受信セットアップ（Discord / Slack / LINE）\n    document.getElementById("dc-setup")?.addEventListener("click", async (e) => {\n      const token = val("dc-token");\n      if (!token) { window.bo.toast("Bot Token を入力してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/discord/setup", { token }, { btn: e.currentTarget, successMsg: null });\n      const el = document.getElementById("dc-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      const yn = (b) => (b ? "✓" : "✗");\n      // P1-11/P2-07：受信URL設定・コマンド登録が両方成功＝設定完了だが、実際の送受信は未確認なので「確認待ち」。\n      // 片方でも失敗なら「一部のみ利用可」と正直に表示。「接続済み」は実テスト成功時の語彙として温存する。\n      const ready = j.interactionsSet && j.commandsRegistered;\n      if (el) el.innerHTML = \\`<div>\\${ready ? \\`✅ \\${CL.pending || "確認待ち"}\\` : \\`⚠️ \\${CL.warning || "一部のみ利用可"}（下記の✗を解消してください）\\`}</div>\\`\n        + \\`<div>App ID 取得・保存：✓ <code>\\${esc(j.appId)}</code></div>\\`\n        + \\`<div>受信URL設定：\\${yn(j.interactionsSet)}\\${j.interactionsSet ? "" : \\` <span class="muted">(手動で <code>\\${esc(j.interactionsUrl)}</code> を設定)</span>\\`}</div>\\`\n        + \\`<div>コマンド登録：\\${yn(j.commandsRegistered)}\\${j.commandsRegistered ? "" : \\` <span class="muted">(\\${esc(j.commandsDetail || "再実行してください")})</span>\\`}</div>\\`\n        + \\`<div class="muted" style="margin-top:.3rem">\\${ready ? "→ STEP 5 で Bot を招待し、実際に話しかけて応答を確認してください。" : "→ 未完了の項目を解消してから STEP 5 へ進んでください。"}</div>\\`;\n      window.bo.toast(ready ? (CL.pending || "確認待ち") : \\`\\${CL.warning || "一部のみ利用可"}（受信URL/コマンド登録を確認）\\`, ready ? "ok" : "err");\n      // STEP 5 の招待ボタンを有効化（App ID 取得済みになったため）。2系統（自動作成あり/既存チャンネル用）を更新。\n      const inv = document.getElementById("dc-invite-btn");\n      const invMin = document.getElementById("dc-invite-btn-min");\n      if (invMin) {\n        // 既に2系統が描画済み（再セットアップ）＝href のみ更新。\n        if (inv && j.inviteUrl) inv.setAttribute("href", j.inviteUrl);\n        if (j.inviteUrlNoCreate) invMin.setAttribute("href", j.inviteUrlNoCreate);\n      } else if (inv && j.inviteUrl) {\n        // 初回（無効ボタンのみ）→ 2系統を注入。\n        inv.outerHTML = \\`<div class="dc-invite-opts">\\`\n          + \\`<a class="btn btn-primary" id="dc-invite-btn" href="\\${esc(j.inviteUrl)}" target="_blank" rel="noopener">招待（baku-office チャンネルを自動作成）</a>\\`\n          + (j.inviteUrlNoCreate ? \\`<a class="btn" id="dc-invite-btn-min" href="\\${esc(j.inviteUrlNoCreate)}" target="_blank" rel="noopener">招待（既存チャンネルだけで使う・作成権限なし）</a>\\` : "")\n          + \\`</div>\\`;\n      }\n      document.getElementById("dc-token").value = "";\n    });\n    document.getElementById("register-discord")?.addEventListener("click", async (e) => {\n      const { ok, data: j } = await window.bo.api("/api/discord/register", {}, { btn: e.currentTarget, successMsg: null });\n      window.bo.toast(ok ? (j.message || "登録しました") : (j.error || "登録に失敗しました"), ok ? "ok" : "err");\n    });\n\n    // ── 常設パネル設置（Discord）：サーバー→チャンネルを選びボタンを投稿・ピン留め\n    const gsel = document.getElementById("dc-panel-guild");\n    const csel = document.getElementById("dc-panel-channel");\n    async function loadGuilds() {\n      if (!gsel) return;\n      const r = await fetch("/api/discord/panel", { credentials: "same-origin" });\n      const j = await r.json().catch(() => ({}));\n      if (!r.ok) { gsel.innerHTML = \\`<option value="">\\${esc(j.error || "読み込みに失敗")}</option>\\`; return; }\n      const gs = j.guilds || [];\n      gsel.innerHTML = gs.length\n        ? \\`<option value="">選択してください</option>\\` + gs.map((g) => \\`<option value="\\${esc(g.id)}">\\${esc(g.name)}</option>\\`).join("")\n        : \\`<option value="">bot が参加しているサーバーがありません（先にサーバーへ追加）</option>\\`;\n    }\n    async function loadChannels(guildId) {\n      if (!csel) return;\n      if (!guildId) { csel.innerHTML = \\`<option value="">サーバーを選択</option>\\`; return; }\n      csel.innerHTML = \\`<option value="">読み込み中…</option>\\`;\n      const r = await fetch("/api/discord/panel?guild=" + encodeURIComponent(guildId), { credentials: "same-origin" });\n      const j = await r.json().catch(() => ({}));\n      const cs = (r.ok && j.channels) || [];\n      csel.innerHTML = cs.length\n        ? \\`<option value="">選択してください</option>\\` + cs.map((c) => \\`<option value="\\${esc(c.id)}">#\\${esc(c.name)}</option>\\`).join("")\n        : \\`<option value="">投稿できるテキストチャンネルがありません</option>\\`;\n    }\n    gsel?.addEventListener("change", (e) => loadChannels(e.currentTarget.value));\n    // §13：Bot Token 未設定（STEP 4 未完）では panel API が 400 を返すため、初期ロードで呼ばない（不要なコンソール400を防ぐ）。\n    if (gsel && gsel.dataset.ready) loadGuilds();\n    // コピー用ボタン（data-copy="#id" の要素テキストをクリップボードへ）\n    document.querySelectorAll("[data-copy]").forEach((b) => b.addEventListener("click", () => {\n      const t = document.querySelector(b.getAttribute("data-copy"))?.textContent || "";\n      navigator.clipboard.writeText(t).then(() => window.bo.toast("コピーしました", "ok")).catch(() => window.bo.toast("コピーできませんでした", "err"));\n    }));\n    function showPanelResult(ok, j, created) {\n      const el = document.getElementById("dc-panel-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      const pin = j.pinned ? "（ピン留め済み）" : "（ピン留めは権限不足のためスキップ。手動でピン留めしてください）";\n      if (el) el.innerHTML = \\`✓ \\${created ? "baku-office チャンネルを用意し、" : ""}パネルを設置しました\\${pin}\\`;\n      window.bo.toast("パネルを設置しました", "ok");\n    }\n    document.getElementById("dc-panel-set")?.addEventListener("click", async (e) => {\n      const channelId = csel?.value;\n      if (!channelId) { window.bo.toast("チャンネルを選択してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/discord/panel", { channelId }, { btn: e.currentTarget, successMsg: null });\n      showPanelResult(ok, j, false);\n    });\n    document.getElementById("dc-panel-create")?.addEventListener("click", async (e) => {\n      const guildId = gsel?.value;\n      if (!guildId) { window.bo.toast("サーバーを選択してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/discord/panel", { guildId, createChannel: true }, { btn: e.currentTarget, successMsg: null });\n      showPanelResult(ok, j, j && j.createdChannel);\n    });\n    // ④ 自分のDiscord連携：Client Secret を保存／コールバック結果(?dc_link=)を表示\n    document.getElementById("dc-link-save")?.addEventListener("click", async (e) => {\n      const inp = document.querySelector(\'input[name="discord_client_secret"]\');\n      if (!inp?.value) { window.bo.toast("Client Secret を入力してください", "err"); return; }\n      await window.bo.api("/api/keys", { discord_client_secret: inp.value }, { btn: e.currentTarget, successMsg: "保存しました" });\n    });\n    (() => {\n      const r = new URLSearchParams(location.search).get("dc_link");\n      if (!r) return;\n      const msg = { ok: "✓ 自分の Discord を連携しました。", dup: "この Discord は別の会員に連携済みです。", state: "連携を中断しました（再度お試しください）。", oauth: "Discord 認可に失敗しました（Client Secret / Redirect URL を確認）。", config: "App ID と Client Secret を先に保存してください。" }[r];\n      const el = document.getElementById("dc-link-result");\n      if (el && msg) { el.textContent = msg; document.getElementById("dc-link-details")?.setAttribute("open", ""); }\n      window.bo.toast(msg || "", r === "ok" ? "ok" : "err");\n    })();\n    document.getElementById("save-keys")?.addEventListener("click", async (e) => {\n      const data = {};\n      document.querySelectorAll("input[name]").forEach((i) => { if (i.value) data[i.name] = i.value; });\n      if (!Object.keys(data).length) { window.bo.toast("変更はありません", "info"); return; }\n      const { ok } = await window.bo.api("/api/keys", data, { btn: e.currentTarget, successMsg: "保存しました" });\n      if (ok) setTimeout(() => location.reload(), 800);\n    });\n    document.getElementById("slack-setup")?.addEventListener("click", async (e) => {\n      const signingSecret = val("sl-secret"), botToken = val("sl-token");\n      if (!signingSecret || !botToken) { window.bo.toast("Signing Secret と Bot Token を入力してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/slack/setup", { signingSecret, botToken }, { btn: e.currentTarget, successMsg: null });\n      const el = document.getElementById("sl-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      if (el) el.innerHTML = \\`<div>✓ \\${CL.saved || "保存済み"}（team: <code>\\${esc(j.team || "")}</code>）<span class="muted">／受信URLは手動設定が必要</span></div><div class="muted">Event Subscriptions の Request URL に <code>\\${esc(j.eventsUrl)}</code> を設定してください。</div>\\`;\n      window.bo.toast(CL.saved || "保存済み", "ok");\n    });\n    document.getElementById("line-setup")?.addEventListener("click", async (e) => {\n      const secret = val("li-secret"), token = val("li-token");\n      if (!secret || !token) { window.bo.toast("シークレットとトークンを入力してください", "err"); return; }\n      const { ok, data: j } = await window.bo.api("/api/line/setup", { secret, token }, { btn: e.currentTarget, successMsg: null });\n      const el = document.getElementById("li-result");\n      if (!ok) { const m = j.error || (CL.error || "エラー"); window.bo.toast(m, "err"); if (el) el.textContent = m; return; }\n      // P1-09/P2-07：Webhook の「URL設定」と「利用ON」の両方が成功＝設定完了（送受信は未確認なので「確認待ち」）。\n      // 以前は webhookSet のみを見て利用ON（webhookActive）の失敗を無視していた＝ON漏れで受信が無音死する穴があった。\n      const whOk = j.webhookSet && j.webhookActive;\n      if (el) el.innerHTML = \\`<div>\\${whOk ? \\`✅ \\${CL.pending || "確認待ち"}\\` : \\`⚠️ \\${CL.warning || "一部のみ利用可"}\\`}（\\${esc(j.botName || j.basicId || "bot")}）</div><div>Webhook URL 設定：\\${j.webhookSet ? "✓ 自動設定済み" : \\`✗ 手動で <code>\\${esc(j.webhookUrl)}</code> を設定\\${j.webhookDetail ? \\` <span class="muted">(\\${esc(j.webhookDetail)})</span>\\` : ""}\\`}</div><div>Webhook 利用：\\${j.webhookActive ? "✓ ON（自動設定済み）" : \\`✗ OFF <span class="muted">— LINE Developers コンソールで「Webhook の利用」を ON にしてください\\${j.webhookActiveDetail ? \\`（\\${esc(j.webhookActiveDetail)}）\\` : ""}</span>\\`}</div>\\${whOk ? \'<div class="muted" style="margin-top:.3rem">→ 自分のLINEから「参加 招待コード」を送り、応答を確認してください。</div>\' : ""}\\`;\n      window.bo.toast(whOk ? (CL.pending || "確認待ち") : \\`\\${CL.warning || "一部のみ利用可"}（Webhook設定を確認）\\`, whOk ? "ok" : "err");\n    });\n\n    // ── 通知（connectors）一覧・追加・削除（プロバイダ別）\n    async function loadConn() {\n      const r = await fetch("/api/connectors", { credentials: "same-origin" });\n      const j = await r.json().catch(() => ({}));\n      const cs = j.connectors || [];\n      for (const prov of ["discord", "slack", "line"]) {\n        const el = document.getElementById("conn-" + prov);\n        if (!el) continue;\n        if (!r.ok) { el.innerHTML = \\`<p class="muted">\\${esc(j.error || "読み込みに失敗")}</p>\\`; continue; }\n        const rows = cs.filter((c) => c.connector === prov);\n        el.innerHTML = rows.length\n          ? \'<table class="tbl"><thead><tr><th>ID</th><th>ラベル</th><th>状態</th><th></th></tr></thead><tbody>\'\n            + rows.map((c) => \\`<tr><td><code>\\${esc(c.id)}</code></td><td>\\${esc(c.label)}</td><td>\\${c.enabled ? "有効" : "無効"}</td><td><button class="btn btn-sm cdel" data-id="\\${esc(c.id)}">削除</button></td></tr>\\`).join("")\n            + "</tbody></table>"\n          : \'<p class="muted">通知先は未登録です。</p>\';\n        el.querySelectorAll(".cdel").forEach((b) => b.addEventListener("click", delConn));\n      }\n    }\n    async function delConn(e) {\n      const id = e.currentTarget.dataset.id;\n      if (!confirm(\\`「\\${id}」を削除しますか？\\`)) return;\n      const { ok, data: j } = await window.bo.api("/api/connectors", { op: "delete", id }, { btn: e.currentTarget, successMsg: null });\n      window.bo.toast(ok ? "削除しました" : (j.error || "削除失敗"), ok ? "ok" : "err");\n      if (ok) loadConn();\n    }\n    for (const prov of ["discord", "slack", "line"]) {\n      document.getElementById("c-add-" + prov)?.addEventListener("click", async (e) => {\n        const id = val("c-id-" + prov), label = val("c-label-" + prov), address = val("c-addr-" + prov);\n        if (!id || !label || !address) { window.bo.toast("用途・ラベル・宛先は必須です", "err"); return; }\n        const { ok, data: j } = await window.bo.api("/api/connectors", { id, label, connector: prov, address }, { btn: e.currentTarget, successMsg: null });\n        window.bo.toast(ok ? "保存しました" : (j.error || "保存失敗"), ok ? "ok" : "err");\n        if (ok) { document.getElementById("c-id-" + prov).value = ""; document.getElementById("c-label-" + prov).value = ""; document.getElementById("c-addr-" + prov).value = ""; saveDraft(); loadConn(); }\n      });\n    }\n    loadConn();\n    })();\n  <\/script>']))) })}`;
}, "/Users/amberlinks/dev/baku-office/apps/client/src/pages/settings/messaging.astro", void 0);
const $$file = "/Users/amberlinks/dev/baku-office/apps/client/src/pages/settings/messaging.astro";
const $$url = "/settings/messaging";
const _page = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: $$Messaging,
  file: $$file,
  prerender,
  url: $$url
}, Symbol.toStringTag, { value: "Module" }));
const page = () => _page;
export {
  page
};
