globalThis.process ??= {};
globalThis.process.env ??= {};
import { c as createComponent } from "./astro-component_Bc18R3r1.mjs";
import { r as renderTemplate, a as addAttribute, b as renderSlot, F as Fragment, c as renderHead, u as unescapeHTML } from "./sequence_BESBTeYg.mjs";
import { r as renderComponent } from "./worker-entry_EC1jLQM3.mjs";
import { env } from "cloudflare:workers";
import { getTheme, brandName } from "./theme_DFty9gzU.mjs";
import { getSession } from "./auth_CKZlflBM.mjs";
var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(cooked.slice()) }));
var _a;
const $$EventPublic = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$props, $$slots);
  Astro2.self = $$EventPublic;
  const { title, desc, brand: brandProp, accent: accentProp, mode: modeProp, eventsHref = "/events", eventsLabel = "イベント", image, canonical, pageType } = Astro2.props;
  Astro2.locals.cspStrict = true;
  const theme = await getTheme(Astro2.locals.ctx).catch(() => ({}));
  const ses = await getSession(env, Astro2.request).catch(() => null);
  const brand = brandProp && brandProp.trim() || brandName(theme);
  const { getSiteNav } = await import("./site-nav_9URm_9uk.mjs");
  const siteNav = await getSiteNav(env).catch(() => ({ menu: [], footer: [], langs: [], builtin: { brand: true, events: false, account: false }, eventsLabel: "イベント", style: "links" }));
  const logoUrl = theme.logoUrl;
  const mode = modeProp ?? (theme.appearance === "dark" ? "dark" : "light");
  const SAFE_COLOR = /^#(?:[0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})$|^(?:rgb|rgba|hsl|hsla)\([0-9.,%\s]+\)$/;
  const accentRaw = accentProp && accentProp.trim() || theme.colors?.brand || (mode === "dark" ? "#c79a5a" : "#9a6a34");
  const accent = SAFE_COLOR.test(accentRaw) ? accentRaw : mode === "dark" ? "#c79a5a" : "#9a6a34";
  const year = 2026;
  const nonce = Astro2.locals.cspNonce;
  const CSP = `default-src 'self'; script-src 'self' 'nonce-${nonce}'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; connect-src 'self'; frame-src https://www.google.com https://www.youtube-nocookie.com; object-src 'none'; base-uri 'self'; form-action 'self'`;
  return renderTemplate(_a || (_a = __template(['<html lang="ja"', "", '> <head><meta charset="utf-8"><meta http-equiv="Content-Security-Policy"', '><meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"><meta name="color-scheme"', ">", "<title>", '</title><link rel="canonical"', '><meta property="og:site_name"', '><meta property="og:title"', ">", '<meta property="og:type"', '><meta property="og:url"', ">", '<meta name="twitter:card"', '><meta name="twitter:title"', ">", "", '<script type="application/ld+json"', ">", "<\/script>", "", "", '</head> <body> <header class="topbar"> <div class="topbar-in"> ', " <nav", "> ", " ", " ", " ", " </nav> </div> </header> ", ' <main class="pagewrap"> ', ' </main> <footer class="footer"> <div class="footer-in"> <span class="fb">', "</span> ", ' <span class="fc">© ', " ", "</span> </div> </footer> ", "  <img", ' alt="" width="1" height="1" style="position:absolute;width:1px;height:1px;opacity:0;pointer-events:none" loading="lazy"> <script', '>\n      (function () {\n        const ov = document.getElementById("ev-login-ov");\n        const openBtn = document.getElementById("ev-login-open");\n        const closeBtn = document.getElementById("ev-login-close");\n        const logoutBtn = document.getElementById("ev-logout");\n        if (logoutBtn) logoutBtn.addEventListener("click", async () => {\n          try { await fetch("/api/login", { method: "DELETE" }); } catch (e) {}\n          location.reload();\n        });\n        if (ov && openBtn) {\n          const open = () => { ov.hidden = false; const i = document.getElementById("lg-id"); if (i) i.focus(); };\n          const close = () => { ov.hidden = true; };\n          openBtn.addEventListener("click", open);\n          closeBtn && closeBtn.addEventListener("click", close);\n          ov.addEventListener("mousedown", (e) => { if (e.target === ov) close(); });\n          document.addEventListener("keydown", (e) => { if (e.key === "Escape" && !ov.hidden) close(); });\n          const go = document.getElementById("lg-go");\n          const msg = document.getElementById("lg-msg");\n          go.addEventListener("click", async () => {\n            const loginId = document.getElementById("lg-id").value.trim();\n            const password = document.getElementById("lg-pass").value;\n            if (!loginId || !password) { msg.textContent = "IDとパスワードを入力してください。"; msg.classList.add("err"); return; }\n            go.disabled = true; msg.classList.remove("err"); msg.textContent = "ログインしています…";\n            try {\n              const r = await fetch("/api/login", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ mode: "local", loginId, password }) });\n              const j = await r.json().catch(() => ({}));\n              if (!r.ok || j.error) { msg.classList.add("err"); msg.textContent = j.error || "ログインに失敗しました。"; go.disabled = false; return; }\n              location.href = "/my-events";\n            } catch (e) { msg.classList.add("err"); msg.textContent = "通信に失敗しました。"; go.disabled = false; }\n          });\n        }\n      })();\n    <\/script> </body> </html>'])), addAttribute(mode, "data-mode"), addAttribute(`--accent:${accent}`, "style"), addAttribute(CSP, "content"), addAttribute(mode === "dark" ? "dark" : "light", "content"), desc && renderTemplate`<meta name="description"${addAttribute(desc, "content")}>`, title, addAttribute(canonical || Astro2.url.href.split("?")[0], "href"), addAttribute(brand, "content"), addAttribute(title, "content"), desc && renderTemplate`<meta property="og:description"${addAttribute(desc, "content")}>`, addAttribute(pageType || "website", "content"), addAttribute(canonical || Astro2.url.href.split("?")[0], "content"), (image || logoUrl) && renderTemplate`<meta property="og:image"${addAttribute(image || new URL(logoUrl, Astro2.url.origin).href, "content")}>`, addAttribute(image ? "summary_large_image" : "summary", "content"), addAttribute(title, "content"), desc && renderTemplate`<meta name="twitter:description"${addAttribute(desc, "content")}>`, (image || logoUrl) && renderTemplate`<meta name="twitter:image"${addAttribute(image || new URL(logoUrl, Astro2.url.origin).href, "content")}>`, addAttribute(nonce, "nonce"), unescapeHTML(JSON.stringify({ "@context": "https://schema.org", "@type": "Organization", name: brand, url: Astro2.url.origin, ...logoUrl ? { logo: new URL(logoUrl, Astro2.url.origin).href } : {} }).replace(/</g, "\\u003c")), siteNav.langs.map((l) => renderTemplate`<link rel="alternate"${addAttribute(l.code, "hreflang")}${addAttribute(new URL(l.href, Astro2.url.origin).href, "href")}>`), renderSlot($$result, $$slots["head"]), renderHead(), siteNav.builtin.brand ? renderTemplate`<a class="brand"${addAttribute(eventsHref, "href")}>${logoUrl ? renderTemplate`<img${addAttribute(logoUrl, "src")}${addAttribute(brand, "alt")}>` : brand}</a>` : renderTemplate`<span class="brand" aria-hidden="true"></span>`, addAttribute("topnav nav-" + siteNav.style, "class"), siteNav.menu.map((m) => renderTemplate`<a${addAttribute(m.href, "href")}>${m.label}</a>`), siteNav.langs.length > 0 && siteNav.langs.map((l) => renderTemplate`<a${addAttribute(l.href, "href")}${addAttribute(l.code, "hreflang")}${addAttribute(l.label, "title")} style="font-size:.82rem;opacity:.8">${l.code.toUpperCase()}</a>`), siteNav.builtin.events && renderTemplate`<a${addAttribute(eventsHref, "href")}>${siteNav.eventsLabel || eventsLabel}</a>`, siteNav.builtin.account && (ses ? renderTemplate`${renderComponent($$result, "Fragment", Fragment, {}, { "default": async ($$result2) => renderTemplate`<a href="/my-events">マイページ</a><button type="button" class="topbtn" id="ev-logout">ログアウト</button>` })}` : renderTemplate`<button type="button" class="topbtn" id="ev-login-open">会員ログイン</button>`), !ses && renderTemplate`<div class="ev-modal-ov" id="ev-login-ov" hidden> <div class="ev-modal card pad" role="dialog" aria-modal="true" aria-labelledby="ev-login-t"> <h2 id="ev-login-t" style="font-size:1.3rem;margin:0 0 4px">会員ログイン</h2> <p class="muted" style="font-size:.86rem;margin:0 0 14px">イベントにお申し込み済みの方は、登録したIDでログインできます。</p> <div class="field"><label>ログインID</label><input class="input" id="lg-id" autocomplete="username"></div> <div class="field"><label>パスワード</label><input class="input" id="lg-pass" type="password" autocomplete="current-password"></div> <button class="btn btn-primary" id="lg-go" style="width:100%">ログイン</button> <div class="formmsg" id="lg-msg" role="status" aria-live="polite"></div> <button type="button" class="ev-modal-x" id="ev-login-close" aria-label="閉じる">閉じる</button> </div> </div>`, renderSlot($$result, $$slots["default"]), brand, siteNav.footer.length > 0 && renderTemplate`<span class="fnav" style="display:flex;gap:14px;flex-wrap:wrap"> ${siteNav.footer.map((m) => renderTemplate`<a${addAttribute(m.href, "href")}>${m.label}</a>`)} </span>`, year, brand, renderSlot($$result, $$slots["scripts"]), addAttribute(`/api/pv?p=${encodeURIComponent(Astro2.url.pathname)}`, "src"), addAttribute(nonce, "nonce"));
}, "/Users/amberlinks/dev/baku-office/apps/client/src/layouts/EventPublic.astro", void 0);
export {
  $$EventPublic as $
};
