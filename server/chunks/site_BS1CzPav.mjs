globalThis.process ??= {};
globalThis.process.env ??= {};
import { c as createComponent } from "./astro-component_Bc18R3r1.mjs";
import { r as renderTemplate, m as maybeRenderHead, F as Fragment, a as addAttribute } from "./sequence_BESBTeYg.mjs";
import { r as renderComponent } from "./worker-entry_EC1jLQM3.mjs";
import { env } from "cloudflare:workers";
import { $ as $$App } from "./App__9dDIE7_.mjs";
import "./stripe_r-RFTlbb.mjs";
import { a as atLeast } from "./types_BVJxqWI9.mjs";
var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(cooked.slice()) }));
var _a;
const prerender = false;
const $$Site = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$props, $$slots);
  Astro2.self = $$Site;
  const { getSession } = await import("./auth_CKZlflBM.mjs");
  const ses = await getSession(env, Astro2.request);
  if (!ses) return Astro2.redirect("/login", 302);
  if (ses.role !== "admin") return Astro2.redirect("/forbidden", 302);
  const { cachedEntitlement } = await import("./client_DbLECgB2.mjs");
  const hasPro = atLeast(await cachedEntitlement(env), "pro");
  const { listSites } = await import("./sites_DXVi6ITP.mjs");
  const sites = hasPro ? await listSites(env) : [];
  const origin = Astro2.url.origin;
  return renderTemplate`${renderComponent($$result, "App", $$App, { "title": "HP/LP 管理", "active": "/settings" }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<h1>HP/LP 管理</h1> <p class="muted" style="font-size:.88rem">公開イベント一覧は <code>${origin}/events</code> で表示されます。専用デザインのLPを使う場合、対応する slug の行をこの一覧から編集でき（見出し・紹介文などを本文として編集）、掲載イベントは<a href="/settings/events">イベント管理</a>と連動します。</p> ${!hasPro && renderTemplate`<div class="card"> <div class="banner banner-warn">この機能は <strong>Pro プラン</strong>で利用できます。</div> <p class="muted">団体のHP/LPをサブパスで公開し、会員申込フォームを会員管理・Stripe と連動できます。</p> <a class="btn btn-primary" href="/billing">プラン・課金へ</a> </div>`}${hasPro && renderTemplate`${renderComponent($$result2, "Fragment", Fragment, {}, { "default": async ($$result3) => renderTemplate` <p class="muted">サブパス公開：トップ＝<code>${origin}/site</code>（slug=home）、各ページ＝<code>${origin}/lp/&lt;slug&gt;</code>。会員申込フォームを表示すると<strong>会員管理に連動</strong>します。</p> <h2>ページ一覧</h2> <div class="table-wrap"><table> <thead><tr><th>slug</th><th>タイトル</th><th>状態</th><th>申込</th><th>操作</th></tr></thead> <tbody> ${sites.map((s) => renderTemplate`<tr${addAttribute(s.slug, "data-slug")}${addAttribute(s.title, "data-title")}${addAttribute(s.body ?? "", "data-body")}${addAttribute(s.published, "data-pub")}${addAttribute(s.show_join, "data-join")}> <td><code>${s.slug}</code></td><td>${s.title}</td><td>${s.published ? "公開" : "下書き"}</td><td>${s.show_join ? "あり" : "—"}</td> <td style="white-space:nowrap"><a class="btn btn-sm btn-primary"${addAttribute("/settings/site/builder?slug=" + s.slug, "href")}>ブロックで編集</a> <button class="btn btn-sm s-edit">HTML編集</button> ${s.published && renderTemplate`<a class="btn btn-sm"${addAttribute(s.slug === "home" ? "/site" : "/lp/" + s.slug, "href")} target="_blank" rel="noreferrer">開く</a>`} <button class="btn btn-sm s-pass">${s.password ? "🔒設定済" : "🔒限定"}</button> <button class="btn btn-sm btn-danger s-del">削除</button></td> </tr>`)} ${sites.length === 0 && renderTemplate`<tr><td colspan="5" class="muted">ページがありません。下で作成してください。</td></tr>`} </tbody> </table></div> <h2 id="editor">ページを作成 / 編集</h2> <div class="card"> <div class="field"><label>slug（英数字。トップは home）</label><input id="s-slug" placeholder="home"></div> <div class="field"><label>タイトル</label><input id="s-title"></div>  <div style="margin:.4rem 0"><button class="btn btn-primary" id="s-block">AI / ブロックエディタで作成 / 編集（おすすめ・HTML不要）</button></div> <p class="muted" style="font-size:.82rem;margin:.2rem 0 .6rem">ブロックを並べるだけでページを作れます。下の「HTMLで直接編集」は上級者向けです。</p> <details class="setup-guide"> <summary>HTMLで直接編集（上級者向け）</summary> <div class="field" style="margin-top:.6rem"><label>本文（HTML可）</label><textarea id="s-body" rows="10" placeholder="<h1>○○会へようこそ</h1><p>……</p>"></textarea></div> <label><input type="checkbox" id="s-pub"> 公開する</label> <label style="margin-left:1rem"><input type="checkbox" id="s-join"> 会員申込フォームを表示</label> <div style="margin-top:.6rem"><button class="btn" id="s-save">HTMLで保存</button></div> </details> </div> <h2>Stripe 連携（任意）</h2> <div class="banner banner-info">カード決済で会員を自動連携する場合：Stripe の Webhook 送信先に <code>${origin}/api/site/stripe-webhook</code> を設定し、連携設定で Stripe のシークレット／Webhook シークレットを登録してください。未設定でも申込は「未払い」で登録され、<strong>現金管理の団体は手動またはエージェントで会員管理</strong>できます。</div> ` })}`} `, "scripts": async ($$result2) => renderTemplate(_a || (_a = __template(['<script data-astro-rerun>\n        const save = document.getElementById("s-save");\n        // 既存 slug 一覧と「編集のために読み込んだ slug」を保持し、新規作成での誤上書きを防ぐ（P2-05）。\n        const existingSlugs = new Set(Array.from(document.querySelectorAll("tr[data-slug]")).map((tr) => tr.dataset.slug));\n        let editingSlug = null;\n        document.querySelectorAll("tr[data-slug]").forEach((tr) => {\n          tr.querySelector(".s-edit")?.addEventListener("click", () => {\n            document.getElementById("s-slug").value = tr.dataset.slug;\n            document.getElementById("s-title").value = tr.dataset.title;\n            document.getElementById("s-body").value = tr.dataset.body;\n            document.getElementById("s-pub").checked = tr.dataset.pub === "1";\n            document.getElementById("s-join").checked = tr.dataset.join === "1";\n            editingSlug = tr.dataset.slug;\n            location.hash = "editor";\n          });\n          tr.querySelector(".s-del")?.addEventListener("click", async (e) => {\n            if (await window.bo.confirm("この公開ページを削除しますか？（公開中の場合は閲覧できなくなります）", { confirmLabel: "削除", danger: true, irreversible: true })) { const r = await window.bo.api("/api/site", { _action: "delete", slug: tr.dataset.slug }, { btn: e.currentTarget }); if (r.ok) setTimeout(() => location.reload(), 400); }\n          });\n          // 限定公開（H7）：パスワード設定/解除。空欄で解除。\n          tr.querySelector(".s-pass")?.addEventListener("click", async (e) => {\n            const pw = window.prompt("このページの閲覧パスワードを入力（空欄＝保護を解除）：", "");\n            if (pw === null) return;\n            const r = await window.bo.api("/api/site", { _action: "set_password", slug: tr.dataset.slug, password: pw }, { btn: e.currentTarget, successMsg: pw.trim() ? "パスワードを設定しました" : "保護を解除しました" });\n            if (r.ok) setTimeout(() => location.reload(), 500);\n          });\n        });\n        save?.addEventListener("click", async (e) => {\n          const slug = (document.getElementById("s-slug").value.trim() || "home");\n          // 新規作成（編集で読み込んでいない slug）なのに既存 slug と一致＝別ページを上書きする恐れ＝確認する（P2-05）。\n          if (slug !== editingSlug && existingSlugs.has(slug)) {\n            if (!(await window.bo.confirm("既存のページ「" + slug + "」を上書きします。よろしいですか？", { confirmLabel: "上書きする", danger: true }))) return;\n          }\n          const r = await window.bo.api("/api/site", { _action: "save", slug, title: document.getElementById("s-title").value, body: document.getElementById("s-body").value, published: document.getElementById("s-pub").checked, show_join: document.getElementById("s-join").checked }, { btn: e.currentTarget, successMsg: "保存しました" });\n          if (r.ok) setTimeout(() => location.reload(), 600);\n        });\n        // P1-19：slug＋タイトルだけでブロックエディタを開く（HTML不要・行が無ければ保存時に作成）。\n        document.getElementById("s-block")?.addEventListener("click", () => {\n          const slug = (document.getElementById("s-slug").value.trim() || "home").toLowerCase().replace(/[^a-z0-9_-]/g, "") || "home";\n          const title = document.getElementById("s-title").value.trim();\n          location.href = "/settings/site/builder?slug=" + encodeURIComponent(slug) + (title ? "&title=" + encodeURIComponent(title) : "");\n        });\n      <\/script>']))) })}`;
}, "/Users/amberlinks/dev/baku-office/apps/client/src/pages/settings/site.astro", void 0);
const $$file = "/Users/amberlinks/dev/baku-office/apps/client/src/pages/settings/site.astro";
const $$url = "/settings/site";
const _page = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: $$Site,
  file: $$file,
  prerender,
  url: $$url
}, Symbol.toStringTag, { value: "Module" }));
const page = () => _page;
export {
  page
};
